import { View, Text, SafeAreaView } from "react-native";
import Card from "../components/Card";

const Feed = () => {
    return (
        <SafeAreaView>
            <Card />
        </SafeAreaView>
    )
}

export default Feed;